## API Report File for "@jupyterlab/mainmenu-extension"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts
import { EditMenu } from '@jupyterlab/mainmenu';
import { FileMenu } from '@jupyterlab/mainmenu';
import { ILabShell } from '@jupyterlab/application';
import { IMainMenu } from '@jupyterlab/mainmenu';
import { IRouter } from '@jupyterlab/application';
import { JupyterFrontEnd } from '@jupyterlab/application';
import { JupyterFrontEndPlugin } from '@jupyterlab/application';
import { KernelMenu } from '@jupyterlab/mainmenu';
import { RunMenu } from '@jupyterlab/mainmenu';
import { SettingsMenu } from '@jupyterlab/mainmenu';
import { TabsMenu } from '@jupyterlab/mainmenu';
import { ViewMenu } from '@jupyterlab/mainmenu';

// @public
export namespace CommandIDs {
  const // (undocumented)
    openEdit = 'editmenu:open';
  const // (undocumented)
    undo = 'editmenu:undo';
  const // (undocumented)
    redo = 'editmenu:redo';
  const // (undocumented)
    clearCurrent = 'editmenu:clear-current';
  const // (undocumented)
    clearAll = 'editmenu:clear-all';
  const // (undocumented)
    find = 'editmenu:find';
  const // (undocumented)
    goToLine = 'editmenu:go-to-line';
  const // (undocumented)
    openFile = 'filemenu:open';
  const // (undocumented)
    closeAndCleanup = 'filemenu:close-and-cleanup';
  const // (undocumented)
    createConsole = 'filemenu:create-console';
  const // (undocumented)
    shutdown = 'filemenu:shutdown';
  const // (undocumented)
    logout = 'filemenu:logout';
  const // (undocumented)
    openKernel = 'kernelmenu:open';
  const // (undocumented)
    interruptKernel = 'kernelmenu:interrupt';
  const // (undocumented)
    restartKernel = 'kernelmenu:restart';
  const // (undocumented)
    restartKernelAndClear = 'kernelmenu:restart-and-clear';
  const // (undocumented)
    changeKernel = 'kernelmenu:change';
  const // (undocumented)
    shutdownKernel = 'kernelmenu:shutdown';
  const // (undocumented)
    shutdownAllKernels = 'kernelmenu:shutdownAll';
  const // (undocumented)
    openView = 'viewmenu:open';
  const // (undocumented)
    wordWrap = 'viewmenu:word-wrap';
  const // (undocumented)
    lineNumbering = 'viewmenu:line-numbering';
  const // (undocumented)
    matchBrackets = 'viewmenu:match-brackets';
  const // (undocumented)
    openRun = 'runmenu:open';
  const // (undocumented)
    run = 'runmenu:run';
  const // (undocumented)
    runAll = 'runmenu:run-all';
  const // (undocumented)
    restartAndRunAll = 'runmenu:restart-and-run-all';
  const // (undocumented)
    runAbove = 'runmenu:run-above';
  const // (undocumented)
    runBelow = 'runmenu:run-below';
  const // (undocumented)
    openTabs = 'tabsmenu:open';
  const // (undocumented)
    activateById = 'tabsmenu:activate-by-id';
  const // (undocumented)
    activatePreviouslyUsedTab = 'tabsmenu:activate-previously-used-tab';
  const // (undocumented)
    openSettings = 'settingsmenu:open';
  const // (undocumented)
    openHelp = 'helpmenu:open';
  const // (undocumented)
    openFirst = 'mainmenu:open-first';
}

// @public
export function createEditMenu(app: JupyterFrontEnd, menu: EditMenu): void;

// @public
export function createFileMenu(
  app: JupyterFrontEnd,
  menu: FileMenu,
  router: IRouter
): void;

// @public
export function createKernelMenu(app: JupyterFrontEnd, menu: KernelMenu): void;

// @public
export function createRunMenu(app: JupyterFrontEnd, menu: RunMenu): void;

// @public
export function createSettingsMenu(
  _: JupyterFrontEnd,
  menu: SettingsMenu
): void;

// @public
export function createTabsMenu(
  app: JupyterFrontEnd,
  menu: TabsMenu,
  labShell: ILabShell | null
): void;

// @public
export function createViewMenu(app: JupyterFrontEnd, menu: ViewMenu): void;

// @public
const plugin: JupyterFrontEndPlugin<IMainMenu>;

export default plugin;

// (No @packageDocumentation comment for this package)
```
